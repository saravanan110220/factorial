# Factorial Program in C

## 📖 Description
This project demonstrates how to calculate the factorial of a non-negative integer using the **iterative method** in C.  
Factorial of a number `n` is defined as:

